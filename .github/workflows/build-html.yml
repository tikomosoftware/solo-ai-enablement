name: Build HTML

on:
  push:
    branches:
      - main
      - master
    paths:
      - "ai-general-101/**/*.md"
      - "ai-general-101/scripts/build-html.js"
      - "ai-starter-kit/**/*.md"
      - "ai-starter-kit/scripts/build-html.js"
      - "ai-dev-advanced/**/*.md"
      - "ai-dev-advanced/scripts/build-html.js"
      - "ai-dev-harness/**/*.md"
      - "ai-dev-harness/scripts/build-html.js"
      - "ai-project-template/**/*.md"
      - "ai-project-template/scripts/build-html.js"
      - "docs-version.json"
      - "package.json"
      - ".github/workflows/build-html.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-html:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Build HTML
        run: npm run build

      - name: Commit generated HTML
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Build HTML from Markdown"
          file_pattern: ai-general-101/html/*.html ai-starter-kit/html/*.html ai-dev-advanced/html/*.html ai-dev-harness/html/**/*.html ai-project-template/html/**/*.html
